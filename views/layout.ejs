<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="<%= typeof metaDescription !== 'undefined' ? metaDescription : 'Portfolio of King Stephen - Fire Fighter, AI/ML Engineer, and Data Analyst specializing in Hazmat Operations, Machine Learning, and Data Analysis.' %>" />
    <meta name="color-scheme" content="light dark" />
    <meta name="robots" content="index, follow" />
    <meta name="author" content="Stephen Wanjiru" />

    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'King Stephen | Fire Fighter & AI/ML Engineer' %></title>

    <!-- Canonical URL -->
    <link rel="canonical" href="<%= typeof canonicalUrl !== 'undefined' ? canonicalUrl : 'https://kingstephen.com' %>" />

    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="<%= typeof ogTitle !== 'undefined' ? ogTitle : (typeof pageTitle !== 'undefined' ? pageTitle : 'King Stephen | Fire Fighter & AI/ML Engineer') %>" />
    <meta property="og:description" content="<%= typeof metaDescription !== 'undefined' ? metaDescription : 'Portfolio of King Stephen - Fire Fighter, AI/ML Engineer, and Data Analyst specializing in Hazmat Operations, Machine Learning, and Data Analysis.' %>" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= typeof canonicalUrl !== 'undefined' ? canonicalUrl : 'https://kingstephen.com' %>" />
    <meta property="og:image" content="<%= typeof ogImage !== 'undefined' ? ogImage : 'https://kingstephen.com/images/profile.jpg' %>" />
    <meta property="og:site_name" content="King Stephen Portfolio" />
    <meta property="og:locale" content="en_US" />

    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="<%= typeof ogTitle !== 'undefined' ? ogTitle : (typeof pageTitle !== 'undefined' ? pageTitle : 'King Stephen | Fire Fighter & AI/ML Engineer') %>" />
    <meta name="twitter:description" content="<%= typeof metaDescription !== 'undefined' ? metaDescription : 'Portfolio of King Stephen - Fire Fighter, AI/ML Engineer, and Data Analyst specializing in Hazmat Operations, Machine Learning, and Data Analysis.' %>" />
    <meta name="twitter:image" content="<%= typeof ogImage !== 'undefined' ? ogImage : 'https://kingstephen.com/images/profile.jpg' %>" />
    <meta name="twitter:creator" content="@ngigicrown" />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />

    <link href="/css/style.css" rel="stylesheet" />
    <!-- Preconnect for faster font loading -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    
    <!-- Google Fonts with font-display: swap for no FOIT -->
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap"
      rel="stylesheet"
    />
    
    <style>
      /* Custom Scrollbar - uses CSS variables for theming */
      ::-webkit-scrollbar {
        width: 8px;
      }
      ::-webkit-scrollbar-track {
        background: var(--color-scrollbar-track);
      }
      ::-webkit-scrollbar-thumb {
        background: var(--color-scrollbar-thumb);
      }
      ::-webkit-scrollbar-thumb:hover {
        background: var(--color-scrollbar-thumb-hover);
      }
      
      /* Lazy Loading Animations */
      .lazy-image {
        opacity: 0;
        transform: translateY(10px);
        transition: opacity 0.6s ease-out, transform 0.6s ease-out;
      }
      
      .lazy-image.loaded {
        opacity: 1;
        transform: translateY(0);
      }
      
      /* Skeleton placeholder for loading images - uses CSS variables */
      .image-skeleton {
        background: linear-gradient(90deg, var(--color-skeleton-1) 25%, var(--color-skeleton-2) 50%, var(--color-skeleton-1) 75%);
        background-size: 200% 100%;
        animation: skeleton-shimmer 1.5s infinite;
      }
      
      @keyframes skeleton-shimmer {
        0% { background-position: 200% 0; }
        100% { background-position: -200% 0; }
      }
      
      /* Smooth image reveal */
      .reveal-on-scroll {
        opacity: 0;
        transform: translateY(20px);
        transition: opacity 0.8s cubic-bezier(0.4, 0, 0.2, 1), transform 0.8s cubic-bezier(0.4, 0, 0.2, 1);
      }
      
      .reveal-on-scroll.visible {
        opacity: 1;
        transform: translateY(0);
      }

      /* Respect user's reduced motion preference */
      @media (prefers-reduced-motion: reduce) {
        .lazy-image,
        .reveal-on-scroll {
          opacity: 1;
          transform: none;
          transition: none;
        }
        
        .image-skeleton {
          animation: none;
        }
      }

      /* Image Protection */
      img {
        user-select: none;
        -webkit-user-drag: none;
        -webkit-touch-callout: none;
      }

      /* Theme-aware utility classes */
      .bg-theme { background-color: var(--color-bg); }
      .bg-theme-card { background-color: var(--color-bg-card); }
      .bg-theme-card-hover:hover { background-color: var(--color-bg-card-hover); }
      .text-theme { color: var(--color-text); }
      .text-theme-secondary { color: var(--color-text-secondary); }
      .text-theme-muted { color: var(--color-text-muted); }
      .border-theme { border-color: var(--color-border); }
      .border-theme-light { border-color: var(--color-border-light); }
      .bg-theme-backdrop { background-color: var(--color-backdrop); }

      /* Nav link hover glow */
      .nav-link {
        transition: text-shadow 0.3s ease, opacity 0.15s ease;
      }
      .nav-link:hover {
        opacity: 1;
        text-shadow: 0 0 8px var(--color-nav-glow), 0 0 20px var(--color-nav-glow);
      }
    </style>

    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "Person",
          "@id": "https://kingstephen.com/#person",
          "name": "Stephen Wanjiru",
          "alternateName": "King Stephen",
          "jobTitle": ["Fire Fighter", "AI/ML Engineer", "Data Analyst"],
          "url": "https://kingstephen.com",
          "image": "https://kingstephen.com/images/profile.jpg",
          "sameAs": [
            "https://www.linkedin.com/in/stephen-wanjiru-163b90149",
            "https://x.com/ngigicrown"
          ],
          "knowsAbout": ["Hazmat Operations", "Artificial Intelligence", "Machine Learning", "Data Analysis", "Fire Fighting"]
        },
        {
          "@type": "WebSite",
          "@id": "https://kingstephen.com/#website",
          "url": "https://kingstephen.com",
          "name": "King Stephen Portfolio",
          "description": "Portfolio of King Stephen - Fire Fighter, AI/ML Engineer, and Data Analyst",
          "publisher": {
            "@id": "https://kingstephen.com/#person"
          }
        }<% if (typeof breadcrumbs !== 'undefined' && breadcrumbs.length > 0) { %>,
        {
          "@type": "BreadcrumbList",
          "itemListElement": [
            <% breadcrumbs.forEach((crumb, index) => { %>
            {
              "@type": "ListItem",
              "position": <%= index + 1 %>,
              "name": "<%= crumb.name %>",
              "item": "<%= crumb.url %>"
            }<%= index < breadcrumbs.length - 1 ? ',' : '' %>
            <% }) %>
          ]
        }
        <% } %>
      ]
    }
    </script>
  </head>
  <body class="font-sans antialiased flex flex-col min-h-screen" style="background-color: var(--color-bg); color: var(--color-text);">
    <%- include('partials/navbar') %>

    <main id="main-content" class="flex-grow px-6 md:px-12 lg:px-24 pt-24 pb-12 transition-opacity duration-500 ease-in-out">
      <%- body %>
    </main>

    <%- include('partials/footer') %>
    
    <!-- Performance: Intersection Observer for smooth image reveals -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Intersection Observer for scroll-triggered animations
        const observerOptions = {
          root: null,
          rootMargin: '50px',
          threshold: 0.1
        };
        
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('visible');
              observer.unobserve(entry.target);
            }
          });
        }, observerOptions);
        
        // Observe all elements with reveal-on-scroll class
        document.querySelectorAll('.reveal-on-scroll').forEach(el => {
          observer.observe(el);
        });
        
        // Handle lazy image loading with fade-in effect and skeleton removal
        document.querySelectorAll('.lazy-image').forEach(img => {
          const handleImageLoad = function() {
            this.classList.add('loaded');
            // Remove skeleton from parent if present
            if (this.parentElement && this.parentElement.classList.contains('image-skeleton')) {
              this.parentElement.classList.remove('image-skeleton');
            }
          };
          
          if (img.complete) {
            handleImageLoad.call(img);
          } else {
            img.addEventListener('load', handleImageLoad);
          }
        });

        // Image Protection: Disable right-click and dragging
        document.querySelectorAll('img').forEach(img => {
          // Disable right-click
          img.addEventListener('contextmenu', (e) => e.preventDefault());
          // Disable dragging
          img.addEventListener('dragstart', (e) => e.preventDefault());
        });
      });
    </script>
  </body>
</html>
